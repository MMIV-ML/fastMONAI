# Fold weights + config + run-ids are generated on the build host by
# prepare_fold_models.py and are not committed (large binaries, and this folder
# is tracked). Regenerate before `docker build` - see README section 3.
vs5f_unet_models/

# Exported docker images (README section 4 writes these here)
*.tar.gz
*.tar

# Python caches
__pycache__/
*.pyc
*.pyo
